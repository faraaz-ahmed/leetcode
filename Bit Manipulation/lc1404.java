class lc1404 {//    class Solution {////        public int getNumberFromBinary(String binary) {//            int pow = 1;//            int num = 0;//            for (int i = binary.length() - 1; i >= 0; i--) {//                if (binary.charAt(i) == '1') {//                    num += pow;//                }//                pow *= 2;//            }//            return num;//        }//        public int numSteps(String s) {//            int num = getNumberFromBinary(s);//            int count = 0;//            while (num != 1) {//                if (num % 2 == 0) {//                    num /= 2;//                } else {//                    num += 1;//                }//                count++;//            }//            return count;//        }//    }    class Solution {        public int numSteps(String s) {            int carry = 0;            int steps = 0;            for (int i = s.length() - 1; i > 0; i--) {                steps++;                if ((s.charAt(i) + carry) == '1') {                    steps++;                    carry = 1;                }            }            if (carry != 0) {                steps++;                if ((s.charAt(0) + carry) == '1') {                    steps++;                }            }            return steps;        }    }}